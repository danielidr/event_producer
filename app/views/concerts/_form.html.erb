<%= form_with(model: concert, local: true) do |form| %>
  <% if concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
      <% concert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :concert_date %>
    <%= form.date_select :concert_date %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.number_field :duration %>
  </div>

  <div class="field">
    <%= form.label :audience %>
    <%= form.number_field :audience %>
  </div>

  <div class="field">
    <%= form.label :group %>
    <%= form.collection_select :group_id, @groups, :id, :name, prompt: "Select group"%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
